server:
  port: 8080
  read_timeout: 30s
  write_timeout: 60s

python_service:
  base_url: "http://python-service:8000"
  timeout: 30s

retrieval:
  fts_top_k: 20
  dense_top_k: 20
  rrf_k: 60
  rerank_top_k: 8
  context_top_k: 5

sqlite:
  path: "/app/data/processed/app.db"

log:
  level: "info"
  format: "json"
