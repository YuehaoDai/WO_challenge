server:
  port: 8080
  read_timeout: 30s
  write_timeout: 180s

python_service:
  base_url: "http://python-service:8000"
  timeout: 120s

retrieval:
  fts_top_k: 30
  dense_top_k: 30
  rrf_k: 60
  rerank_top_k: 12
  context_top_k: 8

sqlite:
  path: "/app/data/processed/app.db"

log:
  level: "info"
  format: "json"
